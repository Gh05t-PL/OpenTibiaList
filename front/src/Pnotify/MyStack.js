import {Stack} from "@pnotify/core";

const MyStack = new Stack({
  dir1: 'up', dir2: 'left', // Position from the top left corner.
  firstpos1: 50, firstpos2: 50 // 90px from the top, 90px from the left.
});

export default MyStack;